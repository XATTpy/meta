</div>
    <div class="w3-center w3-padding-64">
      <div class="w3-bar">
        
        <% if @params['filter'] == 'top100' %>
          <a class="w3-button w3-hover-white"></a>

        <% elsif @params['filter'] != nil and @params['genre'] != nil %>
          <a class="w3-button w3-hover-black" href="?filter=genre&genre=<%= @params['genre'] %>&page=<%= @page - 1 %>">Previous page</a>
          <a class="w3-button w3-hover-black" href="?filter=genre&genre=<%= @params['genre'] %>&page=<%= @page + 1 %>">Next page</a>
        
        <% elsif @params['text'] != nil %>
          <a class="w3-button w3-hover-white"></a>
        
        <% else %>
          <a class="w3-button w3-hover-black" href="?page=<%= @page - 1 %>">Previous page</a>
          <a class="w3-button w3-hover-black" href="?page=<%= @page + 1 %>">Next page</a>
        <% end %>
      
      </div>
    </div>
  </tbody>
</table>